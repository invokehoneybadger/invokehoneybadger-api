# HoneyBadger Vanguard API - Configuration Template
# Copy this file to .env and customize the values

# ============================================================================
# Database Configuration
# ============================================================================
POSTGRES_DB=honeybadger
POSTGRES_USER=hbv
POSTGRES_PASSWORD=CHANGE_ME_SECURE_PASSWORD_HERE

# ============================================================================
# Redis Configuration
# ============================================================================
REDIS_PASSWORD=CHANGE_ME_REDIS_PASSWORD_HERE

# ============================================================================
# API Configuration
# ============================================================================
# Port to expose the API on
API_PORT=3000

# Environment (development, production)
NODE_ENV=production

# JWT secret for token signing (generate with: openssl rand -hex 32)
JWT_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_HEX_32

# Rate limiting (requests per minute per IP)
API_RATE_LIMIT=100

# Logging level (error, warn, info, debug)
LOG_LEVEL=info

# ============================================================================
# Security Notes
# ============================================================================
# 1. Generate strong passwords: openssl rand -base64 32
# 2. Generate JWT secret: openssl rand -hex 32
# 3. Never commit the actual .env file to version control
# 4. Use different credentials for production vs development
# 5. Rotate secrets periodically
# 6. Consider using secret management tools (Vault, AWS Secrets Manager, etc.)

# ============================================================================
# Twingate Integration (Optional)
# ============================================================================
# If using Twingate for zero-trust access:
# 1. Deploy this stack on a server with Twingate Connector installed
# 2. No need to expose API_PORT publicly
# 3. Access API via Twingate: http://internal-server-ip:3000
# 4. HBV tools will route through Twingate automatically
